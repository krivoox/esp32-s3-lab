# include/

Solo plantillas y (localmente) secretos.

| Archivo | Tracked | Rol |
|---------|---------|-----|
| `secrets.h.example` | sí | Plantilla Wi‑Fi |
| `secrets.h` | **no** (gitignore) | Credenciales reales |

Headers de firmware viven junto a cada módulo en `src/`.
