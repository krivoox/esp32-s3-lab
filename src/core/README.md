# Core — hardware compartido

| Archivo | Rol |
|---------|-----|
| `board_pins.h` | Pines Waveshare ESP32-S3-LCD-1.47B + tamaño LCD |
| `qmi8658.h` / `.cpp` | IMU (acelerómetro / gyro) |

Wi‑Fi y secretos: `include/secrets.h` (local, gitignored) desde `include/secrets.h.example`.
